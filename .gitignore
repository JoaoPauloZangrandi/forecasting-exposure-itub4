# =============================================================================
# Dados brutos da CVM — grandes (~4,2 GB), NÃO versionar.
# Ficam em C:\Users\joaoz\Downloads\Consolidado_MF\Consolidado_MF e são
# referenciados por DATA_DIR em R/00_config.R. Ver data/raw/README.md.
# =============================================================================
data/raw/*.csv
data/raw/**/*.csv

# Cache binário dos extracts (grande; reconstruível). O painel .csv É versionado.
data/processed/_cache_*.rds

# Lista de arestas da CDA (grande ~57 MB; regenerável por R/10_build_cda_edges.R).
data/processed/cda_edges.csv

# --- R ---
.Rhistory
.RData
.Rapp.history
.Rproj.user/
*.Rproj

# --- LaTeX (temporários de compilação; o .pdf final É versionado) ---
*.aux
*.log
*.bbl
*.blg
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.run.xml
*.bcf
*.nav
*.snm
*.vrb

# --- Sistema operacional ---
Thumbs.db
.DS_Store
desktop.ini
